<resources>
    <!-- Base application theme. -->
    <style name="Theme.Techo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.Techo.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Techo.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FloatingButton" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="iconTint">@color/fabIcon</item>
        <item name="backgroundTint">@color/fabBackground</item>
        <item name="fabSize">normal</item>
        <item name="elevation">6dp</item>
    </style>

    <style name="FloatingSubButton" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="iconTint">@color/fabIcon</item>
        <item name="backgroundTint">@color/fabBackground</item>
        <item name="fabSize">mini</item>
        <item name="elevation">4dp</item>
        <item name="tint">@color/fabIcon</item>
    </style>

    <style name="EditPanelCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/editPanelCard</item>
        <item name="cardCornerRadius">10dp</item>
        <item name="cardElevation">8dp</item>
    </style>

    <style name="DialogActivity" parent="Theme.Techo">
        <!-- 背景色透明度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明,设置为false无透明效果 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 窗口样式Dialog -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>
</resources>