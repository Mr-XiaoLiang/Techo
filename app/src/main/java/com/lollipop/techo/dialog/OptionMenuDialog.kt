package com.lollipop.techo.dialog

import android.content.Context

/**
 * OptionMenu默认上下文选择菜单现在用的是系统自带的，不方便切换样式
 * 还是自己写的好控制一些
 */
class OptionMenuDialog(context: Context) : BaseBottomSheetDialog(context) {
    // TODO 需要手动解析Menu信息
}